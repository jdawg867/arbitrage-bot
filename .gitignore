node_modules
.env
# Live config (per-machine: your address, mode, keys' companion settings).
# Copy config.example.json -> config.json and edit. Kept out of git so your
# production settings never conflict on pull.
config.json
coverage
coverage.json
typechain
typechain-types

#Hardhat files
cache
artifacts

# Claude Code local settings
.claude/settings.local.json

# Trade logs (your private financial records)
logs

